apiVersion: v1
kind: Service
metadata:
  name: {{ include "dipService.fullname" . }}
  labels:
    {{- include "dipService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dipService.selectorLabels" . | nindent 4 }}
