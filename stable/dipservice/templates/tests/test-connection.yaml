#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ include "dipservice.fullname" . }}-test-connection"
#  labels:
#    {{- include "dipservice.labels" . | nindent 4 }}
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['{{ include "dipservice.fullname" . }}:{{ .Values.spec.ports.port }}']
#  restartPolicy: Never
