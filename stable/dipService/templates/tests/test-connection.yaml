#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ include "dipService.fullname" . }}-test-connection"
#  labels:
#    {{- include "dipService.labels" . | nindent 4 }}
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['{{ include "dipService.fullname" . }}:{{ .Values.service.port }}']
#  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "dipService.fullname" . }}
  labels:
    {{- include "dipService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dipService.selectorLabels" . | nindent 4 }}

